# L2 consensus latency/throughput bench — Pi 5 BARE-METAL twin of
# `consensus-bench.yaml` (`.context/rfc_performance_benchmarking.md` §6).
# Full single-node consensus pipeline with a `consensus_bench` injector
# feeding consensus.proposals directly. propose→commit latency and
# commit throughput read from the instrumented producers (consensus
# commit_latency_ms and commit_advances) over /metrics
# (scrape :19090 over the wire) + log_net UDP:6666.
#
# Differs from the linux twin only in: target pi5, `platform.debug.to: net`
# (log_net observation), and `ip` (not `linux_net`) as the net module.
#
# ⚠ Bare-metal has no dev_fs provider yet — the WAL degrades to in-memory
# (FS_OPEN_CREATE → ENODEV; see single-pi5-throughput.yaml). So this
# measures the CONSENSUS + SCHEDULER path on real silicon, NOT a
# disk-durable commit. Re-take when the fluxor pi5 NVMe provider lands.
#
# Validated 2026-06-16 on pi5-a (RFC §14): below the knee tracks offered
# rate at p50≈4–6 ms; saturating plateau ≈1000–1060/s committed, inflight
# pinned at the cap (24), commit_index sane, lossless — the durability-
# backpressure fix holds on bare metal.
#
# ⚠ Sweep mechanics: raise offered load via `inject_period` (or a SMALL
# `batch_per_step`), NOT a large batch. The injector is itself an on-DUT
# cooperative module; a heavy `batch_per_step` (e.g. 8) burns tick time and
# STARVES the commit/apply pipeline — a measurement artifact (8000/s
# offered@batch=8 collapsed achieved to ~270/s while still lossless). Keep
# batch ≤2. offered/s ≈ batch_per_step / inject_period × 1000.
#
# DEV/TEST ONLY — the HTTP admin surface is reachable without a proxy.
target: pi5
tick_us: 1000

module_search_paths:
  - ../../clustor/modules/app

scheduler:
  accept_cycles: true

platform:
  debug:
    to: net
  net: {}
  # Bring up NVMe-backed fat32 so the WAL's FS_OPEN_CREATE resolves to a real
  # fd (not ENODEV→in-memory). The storage stack (stacks/storage.toml) injects
  # nvme(bind=m2_primary)+fat32 modules and their wiring. Without this the WAL
  # can't ack durability → commit_index stays 0 (the bare-metal gap).
  storage:
    media: nvme

modules:
  # L2 load injector — drives proposals into consensus.proposals.
  - name: consensus_bench
    params:
      total: 120000
      body_size: 64
      batch_per_step: 1
      # Default 1 proposal every 4 ms ≈ 250/s — well below the ~1000/s
      # bare-metal knee, so it completes cleanly (4000 committed, p50≈6 ms)
      # as an "it works" smoke. To sweep the knee, drop inject_period
      # toward 1 (1000/s) and beyond into the plateau (see header).
      inject_period: 4
      warmup_ms: 2000
  - name: peer_router
    params:
      self_id: 0
      peer_count: 1
      listen_port: 9090
      peer0_port: 9090
  - name: gateway
    params:
      self_id: 0
  - name: consensus
    params:
      self_id: 0
      voter_count: 1
      heartbeat_interval_ms: 150
      peer_count: 1
  - name: durability
    params:
      # Production defaults are window=2/max_pending=64; the test
      # caps pending at 4 so a handful of admin proposals trips the
      # threshold flush within the test window.
      fsync_mode: 1
      group_window_ms: 10
      group_max_pending: 32
      # Write segments to the FAT32 ROOT with 8.3 names (no wal/ subdir —
      # FAT32 has no mkdir). Required for FS_OPEN_CREATE to succeed on the
      # NVMe fat32 fs; without it the WAL can't open a segment and stays
      # in-memory (ws=0, commit_index=0).
      root_path: 1
      # Start fresh: skip boot replay of on-disk segments. Without this, a warm
      # restart replays the prior run's ~4680 entries and then the durability→
      # commit chain stalls 1 entry later ([raft] wal full) — a replay-continue
      # wedge unrelated to storage. Fresh start measures clean durable throughput.
      skip_replay: 1
      self_id: 0
      voter_count: 1
      partition_id: 0
  - name: control_plane
  - name: admission
  # Binds on the same `ip` as `peer_router` (CMD_BIND on a
  # distinct port). See `configs/single.yaml`.
  - name: operations
    params:
      emit_interval_ms: 250
      listen_port: 19090

wiring:
  - from: ip.net_out
    to: peer_router.net_in
  - from: peer_router.net_out
    to: ip.net_in

  - from: peer_router.cleartext
    to: gateway.requests
  - from: peer_router.peer_rx
    to: consensus.ack
  - from: peer_router.raft_rpc
    to: consensus.rpc
  - from: gateway.admin_req
    to: operations.admin_req
  - from: gateway.responses
    to: peer_router.client_resp
  - from: gateway.proposals_tagged
    to: consensus.proposals_tagged
  - from: consensus.proposal_assigned
    to: gateway.proposal_assigned
  - from: consensus.leader_state
    to: gateway.leader_state

  - from: admission.credits
    to: gateway.credit_supply
  - from: admission.envelope
    to: gateway.throttle_status
  - from: consensus.net_out
    to: peer_router.peer_tx
  - from: consensus.lag_signal
    to: admission.lag
  - from: consensus.rpc_out
    to: peer_router.peer_tx

  - from: consensus.log_append
    to: durability.entries
  # A restarted replica reads its replayed log bodies back out of the
  # local WAL: replay restores the log, so log-matching succeeds and the
  # leader never re-ships those entries. Without this pair the apply path
  # stalls at the first gap.
  - from: consensus.entry_request
    to: durability.entry_request
    buffer_bytes: 8192
  - from: durability.entry_reply
    to: consensus.entry_reply
    buffer_bytes: 8192
  - from: durability.flushed
    to: consensus.wal_flushed
  # Disk-recovery edge: hands raft the replayed on-disk high-water once the WAL
  # finishes boot replay. Without it, a warm restart with a persistent (NVMe)
  # WAL leaves raft holding proposal intake forever — the replayed uncommitted
  # tail (> MAX_UNCOMMITTED_INFLIGHT) never commits and the broker wedges. The
  # consensus-bench config omitted this (in-memory WAL never replays); required
  # once storage.media=nvme makes the WAL persistent.
  - from: durability.replay_complete
    to: consensus.wal_replay_complete
  - from: consensus.cross_durability_ack
    to: durability.ack
  - from: durability.quorum_durable
    to: consensus.durable
  - from: gateway.reads
    to: consensus.read
  - from: consensus.applied
    to: gateway.applied

  - from: durability.export_chunks
    to: consensus.snapshot_rx
  - from: consensus.snapshot_import
    to: durability.import_chunks
  - from: durability.manifest_auth
    to: peer_router.peer_tx
  - from: durability.installed_local
    to: consensus.snapshot_installed

  - from: control_plane.proof
    to: admission.proof
  - from: admission.cache_state
    to: consensus.cp_state
  # NB: admission.strict_fallback → consensus.cp_state is intentionally
  # omitted here. With no real control plane feeding control_plane, the
  # proof cache ages out of Fresh after ~1 s and would latch raft into
  # strict fallback (dropping proposals). For an isolated L2 consensus
  # bench we keep raft appending; commit safety still rests on quorum
  # durability (durability's ledger → consensus's commit horizon).
  - from: admission.permits
    to: consensus.read_permits
  - from: control_plane.routing
    to: gateway.placement
  - from: operations.denied
    to: gateway.admin_responses
  - from: operations.raft_commands
    to: consensus.admin_proposals
  - from: consensus.admin_applied
    to: operations.admin_applied
  - from: operations.responses
    to: gateway.admin_responses
  - from: operations.raft_proposal
    to: consensus.proposals

  # L2 injector → raft proposals (untagged port; coexists with the
  # operations.raft_proposal fan-in already wired to this port).
  - from: consensus_bench.proposals
    to: consensus.proposals
  - from: consensus_bench.metrics
    to: operations.ingest

  - from: consensus.metrics
    to: operations.ingest
  - from: durability.metrics
    to: operations.ingest
  - from: admission.metrics
    to: operations.ingest
  - from: peer_router.metrics
    to: operations.ingest
  - from: gateway.metrics
    to: operations.ingest
  - from: operations.readyz
    to: gateway.readyz_data
  - from: operations.why
    to: gateway.why_data
  - from: operations.export
    to: gateway.metrics_data

  - from: ip.net_out
    to: operations.net_in
  - from: operations.net_out
    to: ip.net_in
